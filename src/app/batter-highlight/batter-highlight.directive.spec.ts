import { BatterHighlightDirective } from './batter-highlight.directive';

describe('BatterHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new BatterHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
